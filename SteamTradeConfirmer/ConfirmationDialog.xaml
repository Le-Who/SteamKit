<Window x:Class="SteamTradeConfirmer.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подтверждение Steam Guard" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Подтверждение Steam Guard" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Steam требует дополнительное подтверждение для входа в аккаунт:" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="2" x:Name="AccountTextBlock" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="3" x:Name="InstructionTextBlock" Text="Введите код подтверждения из мобильного приложения Steam:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="4" x:Name="ConfirmationCodeTextBox" Margin="0,0,0,15" MaxLength="5" FontSize="16" TextAlignment="Center"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Отмена" Click="CancelButton_Click" Margin="0,0,10,0" MinWidth="80"/>
            <Button Content="Подтвердить" Click="ConfirmButton_Click" IsDefault="True" MinWidth="80"/>
        </StackPanel>
    </Grid>
</Window>
